PROJECT(Document)

CMAKE_MINIMUM_REQUIRED(VERSION 2.4)

INCLUDE(UseLATEX.CMAKE)

SET(LATEX_OUTPUT_PATH "out")

ADD_LATEX_DOCUMENT(Document.tex
	INPUTS ch1.introduccion.tex ch2.antecedentes.tex
	IMAGES evolucion-gpu.png
	DEFAULT_PDF
	USE_METAPOST)